# Vars
export LANG=en_US.UTF-8
export EDITOR="nvim"
export PATH="$HOME/.local/bin:$HOME/.config/scripts:$PATH"
export PKG_CONFIG_PATH=/usr/local/lib/pkgconfig:$PKG_CONFIG_PATH
export LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH
export TERMINAL=foot
export BROWSER=zen-browser

# Alias
alias vi="nvim"
alias vim="nvim"
alias zshconf="vi ~/.zshrc"
alias zshref="source ~/.zshrc"
alias ls='eza --git-ignore'
alias ll='eza -la'
alias g="git"
alias cls="clear"
alias lg="lazygit"
alias lj="lazyjournal"
alias em='emacsclient -c -a "emacs"'
alias get="wget"
alias open="xdg-open"
alias clip="wl-copy"
alias define='_def(){ curl -s "dict://dict.org/d:$1" | bat --language=md --style=grid,numbers; }; _def'

alias sv="systemctl"
alias music="systemctl --user start {mpd,mpd-mpris}"
alias musicoff="systemctl --user stop {mpd,mpd-mpris}"

alias dwmgen="cd ~/.ashdwm/dwm/ && sudo make clean install"
alias dwlgen="cd ~/.ashdwl/dwl/ && sudo make clean install"
alias pdry="patch --dry-run -p1 <"
alias prun="patch -p1 <"
alias sx="startx ~/.xinitrc"
alias ws="wayback-session"
alias sus="systemctl suspend"
